<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Customize dialog background, text color, etc. -->
        <item name="android:windowBackground">@drawable/radius</item>
        <item name="android:textColor">#FFFFFF</item>


        <!-- Add more customizations as needed -->
    </style>

    <style name="AlertDialogCustomMessage" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#BCBCBC</item>
    </style>
</resources>